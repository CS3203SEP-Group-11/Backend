spring:
  application:
    name: api-gateway

jwt:
  secret: Zx9f2@qL1!mYkBv7$P0tWnRzX4eU8gJhCq3LsDa6#FtVbMpN

server:
  port: 8080

# Actuator configuration for health checks
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized

# Default CORS Configuration (overridden by profiles)
cors:
  allowed-origins:
    - "http://localhost:3000"
